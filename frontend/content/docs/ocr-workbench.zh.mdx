---
title: OCR 工作台
description: 对扫描版 PDF 执行 OCR 翻译并导出结果。
---

## 何时使用 OCR

当 PDF 主要是图片扫描页、常规文本抽取效果不可靠时，应使用 OCR。

## OCR 流程

1. 在 `/ocrtranslator` 打开已上传文件。
2. 确认源语言和目标语言。
3. 启动 OCR 任务，等待解析与翻译完成。
4. 检查结果并导出。

## 交互规则

- OCR 页面存在活动文档/任务上下文时，禁止再次上传文件。
- Translate 检测到高置信扫描件时，会直接引导用户进入 OCR 页面。
