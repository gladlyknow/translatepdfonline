# BabelDOC FC: HTTP entry + R2 upload. BabelDOC itself must be installed in the image (e.g. pip install -e /code/tmp/BabelDOC).
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
boto3>=1.35.0
pypdf>=5.0.0
